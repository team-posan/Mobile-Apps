import React from "react";
import { StyleSheet } from "react-native";

const text = StyleSheet.create({
  textHeader: {
    width: 250,
    fontSize: 25,
    textAlign: "center",
    fontWeight: "bold",
    color: "#1E2749",
  },
  textButton: {
    fontSize: 18,
    textAlign: "center",
    fontWeight: "bold",
    color: "#fff",
  },
});

export default text;
